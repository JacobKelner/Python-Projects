from django.contrib import admin
from .models import UniversityClasses

# Register your models here.
admin.site.register(UniversityClasses) # Allows for models to be added to the Django admin to create, delete, update, etc.

